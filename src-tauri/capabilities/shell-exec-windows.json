{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-exec-windows",
  "description": "插件进程执行（Windows）：仅登记 `cmd.exe`，但 args 全开 —— 插件传 `cmd.exe /C <命令>` 即等价执行任意命令。scope 的 `name` 必须等于插件传给 Command.create 的程序名，`cmd` 才是真正启动的程序。",
  "platforms": ["windows"],
  "windows": ["*"],
  "permissions": [
    "shell:allow-kill",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "cmd.exe", "cmd": "cmd.exe", "args": true }]
    }
  ]
}
